# VHDL Testbenches

Native VHDL testbenches and VHDL-only simulation helpers live here.

The existing SystemVerilog testbenches in `../` remain the primary golden
regressions. The current top-level regression uses a VHDL encoder hierarchy
while reusing the existing stimulus and output checks.
