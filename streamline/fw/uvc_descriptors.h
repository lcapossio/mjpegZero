// SPDX-License-Identifier: Apache-2.0
// uvc_descriptors — the device's descriptor bundle (see uvc_descriptors.c).
// Byte-exactness against the baseline golden set is machine-checked:
// fw/gen/Makefile `make check`.
#ifndef UVC_DESCRIPTORS_H
#define UVC_DESCRIPTORS_H
#include "uvc_class.h"
extern const uvc_descriptors_t uvc_descriptors_yuy2;
#endif
